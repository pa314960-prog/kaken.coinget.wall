# 開発進捗メモ (シルエットコインゲット)

このファイルは、Claude Code などの AI エージェントがローカル環境で
作業を引き継ぐ際に読み込む前提の進捗管理ファイルです。
人が読んでも良いように日本語で状態を書いています。

## ゲーム仕様（最終目標）

- 制限時間60秒。落ちてくるコインを手で弾き飛ばすとスコア加算
- 手を速く動かして弾くほど高得点（勢いに応じて倍率が上がる）
- 床に落とす前に空中で連続して弾くとコンボ（最大5倍）
- 弾いた回数が一定数たまると BIG 状態になり得点2倍、色が変わる
- 感度と落下速度をスライダーで調整可能
- USJ「クッパJr.ファイナルバトル」のように、プレイ中は実写カメラ映像
  そのものではなく、明るいステージ背景の上に真っ黒な影（シルエット）
  だけを表示する
- 特別なビルド手順・外部ライブラリ無しで、他の PC でもそのまま動く
  （Vanilla HTML/CSS/JS のみ。npm install 等が不要）

## ステージ構成と現在の状態

| Stage | 内容 | 状態 |
|---|---|---|
| 1 | カメラ取得 + 鏡像表示 | ✅ 実装済み |
| 2 | 背景記憶ボタン + 背景差分マスク可視化 | ✅ 実装済み |
| 3 | ゲームロジック（落下物・弾き飛ばし・スコア・タイマー・スライダー） | ✅ 実装済み |

全ステージ実装済みで、一通り遊べる状態です。細部の調整（当たり判定の
精度、スコアバランスなど）は実際にブラウザで動かしながら詰めていく
想定です。

## Stage 1 の実装内容

- `index.html`: カメラ映像を表示するステージと開始/停止ボタン。
- `game.js`:
  - `getUserMedia` でカメラ映像を取得。
  - `<video>` は非表示にし、`<canvas id="view">` に毎フレーム
    左右反転（鏡像）して描画する。
  - カメラ権限エラー時はステータス欄にエラーメッセージを表示する。
  - `getUserMedia` が使えない接続（file:// など）の場合は開始ボタンを
    無効化し、案内メッセージを出す。

## Stage 2 の実装内容

- 縮小解像度 (160x120) のオフスクリーン canvas (`procCanvas`) を用意し、
  そこに毎フレーム鏡像を描画してグレースケール輝度配列に変換する
  （`captureMirroredLuma`）。処理負荷を抑えるため、当たり判定・
  マスク計算はすべてこの縮小解像度で行う。
- 「② 背景を記憶」ボタン (`captureBackground`) を押した瞬間の輝度配列を
  `backgroundLuma` として保持する。
- 毎フレーム、現在の輝度と `backgroundLuma` との差分を計算し、
  感度スライダーの値 (`sensitivityThreshold`) を超えたピクセルを
  シルエットと判定して `silhouetteMask` (0/1 の配列) に格納する
  (`updateSilhouetteMask`)。
- プレイ開始前（カメラ確認〜背景記憶の段階）は実写映像を表示し、
  「影の検出範囲を緑色で確認する」チェックボックスで、マスクを半透明
  カラーのオーバーレイとして重ねて可視化・感度調整できる
  (`drawMaskOverlayIfNeeded`)。

## USJ風の影表示（追加実装）

- プレイ中〜ゲーム終了後は `drawMirroredCameraFrame` を呼ばず、代わりに
  `drawStageBackground` と `drawSilhouetteShadow` を呼ぶように変更した。
  実写カメラ映像そのものはプレイ中に一切表示されない。
- 実際の USJ の映像に合わせ、「明るい逆光スクリーンの上に、自分が
  真っ黒なシルエットとして抜ける」表現にしている。
  - `buildStageBackground`: オレンジ系の明るいグラデーション +
    背後から差し込む「光の柱」（縦縞）+ 照明のボケ（光の玉）。
    キャンバスサイズ確定時に一度だけ生成して使い回す。
  - `drawSilhouetteShadow`: 背景差分マスクを `SILHOUETTE_COLOR_*`
    （ほぼ黒。BIG中はやや金寄り）で塗り、`shadowColor` に
    `SILHOUETTE_GLOW_*`（通常は暖色 / BIG中は金）を指定して
    輪郭のまわりに光を滲ませる。
  - コイン(🪙)は弾く目標なので、明るい背景でも見失わないよう
    `shadowBlur` で光らせている。弾かれた後は回転させて飛ばす。
- プレイ開始前（カメラ確認〜背景記憶）は引き続き実写映像を表示し、
  位置合わせや感度調整をしやすくしている。

## マスク後処理（影を「完全な影」にするための処理）

単純な背景差分だけだと、服や肌の明るさがたまたま背景と近い部分が
「背景」と判定され、体の内側がまばらに虫食い状に抜けた影になる。
そこで `updateSilhouetteMask` の最後に `refineSilhouetteMask` を呼び、
毎フレーム次の処理を行っている（`game.js` の「マスク後処理」セクション）。

1. `dilateMask` で膨張させ、バラバラになった断片を一つに繋ぐ
2. `fillMaskHoles` で、画面外周から到達できない 0 の領域＝体の内側の穴を塗る
3. `erodeMask` で収縮させ、元の太さに戻す
4. もう一度 `fillMaskHoles` で、収縮により開き直した穴を埋める
5. `removeSmallBlobs` で、体から離れた小さな塊をノイズとして消す

膨張・収縮は横方向と縦方向に分けた移動和で実装しており、
穴埋め・ノイズ除去はスタックによる塗りつぶし（flood fill）で行う。
作業用バッファは毎フレーム確保せず使い回している。

調整用の定数:

- `MASK_CLOSE_RADIUS` (既定 3): 抜けを塞ぐ強さ。大きいほど影が太く、
  ディテールは失われる。影がまだ虫食いになるなら上げる。
- `MASK_MIN_BLOB_PX` (既定 60): これ未満の孤立した塊をノイズとして消す。
  画面にゴミが出るなら上げる。ただし上げすぎると、体から離れた手なども
  消えてしまう点に注意。

処理コストは 160x120 の解像度で 1 フレームあたり 0.7ms 程度
（虫食いさせた人型で計測。60fps の 16.7ms に対して十分小さい）。

このマスクは描画だけでなく、コインを弾く判定
(`measureSilhouetteMotionAt`) にも使われるため、穴が埋まったことで
「当てたのに弾けない」という取りこぼしも起きにくくなっている。

なお `drawSilhouetteShadow` では、低解像度マスクを拡大したときの輪郭の
カクつきを抑えるため、`ctx.filter` で軽くぼかしてから描画している。

## Stage 3 の実装内容（コインを手で弾き飛ばすゲーム）

当初は「影で触れてコインをキャッチする」内容だったが、
「落ちてくるものを手で飛ばす」ゲームに変更した。敵(👾)は廃止し、
それに紐づいていたパワーダウン機能も削除済み。

### 手の動きの検出 (`measureSilhouetteMotionAt`)

コインの位置のまわりを調べ、次の2つを返す。

- `touching`: 半径 r の内側のシルエット被覆率が `TOUCH_COVERAGE` 以上か
- `dirX/dirY`, `power`: 手が動いた向きと勢い

向きは「前フレームには無く今フレームに現れた画素（手が入ってきた側）の重心」
と「前フレームにあって今フレームで消えた画素（手が抜けた側）の重心」の差から
求める。そのため1フレーム前のマスク `prevSilhouetteMask` を保持している
（`updateSilhouetteMask` で2つのバッファを入れ替えて使い回す）。

**重要**: 動きの検出は、当たり判定より `MOTION_RADIUS_MUL` 倍広い円で行う。
判定円と同じ広さで見ると、コインより大きい手が円を覆い尽くしたときに
画素の出入りが起きず、「動いていない」と誤判定される（実際に実装中に
この不具合が出たため広げた）。

### 弾き飛ばし (`flingCoin`) と物理

- 横方向は手が動いた向きへ、縦方向は**必ず上向き**に跳ね上げる。
  カメラ越しの操作では細かい向きを狙いにくいため、
  「弾いたら上に飛ぶ」と決め打ちにした方が気持ちよく遊べる。
- 勢い `power` (0〜1) が大きいほど上向きの速度と得点が上がる。
- コインは重力で落下し、左右の壁と天井で跳ね返る（画面内に留まるので
  連続で弾きやすい）。床より下に落ちたら見逃しとして消える。
- 一度弾いてから `HIT_COOLDOWN_MS` の間は再度弾けない
  （同じ振りで何度も当たってしまうのを防ぐ）。

### スコアとコンボ

- 得点 = `COIN_SCORE * (1 + power) * コンボ倍率 * (BIG中は2)`
- コンボ倍率は、そのコインを床に落とさず連続で弾けた回数（上限 `COMBO_MAX`）。
- 弾いた回数が `BIG_EVERY_N_HITS` に達するごとに BIG 状態
  （`BIG_DURATION_MS` の間、得点2倍・影とコインが金色に光る）。
- 弾いた瞬間は `hitEffects` に広がる輪と獲得点数を積み、`drawHitEffects` で描画。
- 60秒の制限時間タイマーで終了し、最終スコア・弾いた回数・最高コンボを表示する。

### 調整用の定数（`game.js` の「弾き飛ばしの調整値」）

- `GRAVITY`: 重力。落下速度スライダーの倍率がかかる。
- `FLING_UP_MIN` / `FLING_UP_MAX`: 弾いたときに上へ跳ね上がる速さの下限・上限。
  ゆっくり手を動かしても飛ぶよう、下限は大きめ。
- `FLING_SIDE_MAX`: 手が動いた向きへ与える横方向の速さ。
- `HIT_COOLDOWN_MS`: 連続ヒット防止の待ち時間。
- `COMBO_MAX`: コンボ倍率の上限。
- `MOTION_RADIUS_MUL` / `MOTION_FULL_POWER_RATIO`: 動きの検出範囲と、
  「最大の勢い」とみなす画素の入れ替わり割合。手の動きに反応しにくい場合は
  `MOTION_FULL_POWER_RATIO` を下げる。

### 動作確認方法（人間が実際にブラウザで確認する）

1. リポジトリのルートで簡易 HTTP サーバーを起動する
   （例: `python -m http.server 8000`）。
2. ブラウザで `http://localhost:8000/` を開く。
3. 「① カメラ開始」→カメラ権限を許可→自分の映像が鏡のように映ることを確認。
4. 画面から自分が外れた状態で「② 背景を記憶」を押す。
5. 「シルエットマスクを表示」にチェックを入れ、画面内に戻って
   体を動かし、自分の輪郭に緑色のマスクが追従することを確認する。
   マスクがちらつく/反応しない場合は感度スライダーで調整する。
6. 「③ ゲーム開始」を押し、落ちてくるコインを手で払うように弾いてみる。
   - 弾いた瞬間に輪のエフェクトと「+点数」が出ること
   - 手を動かした向きにコインが飛び、上に跳ね上がること
   - 床に落ちる前にもう一度弾くと「x2」「x3」とコンボが上がること
7. 60秒後にゲーム終了画面が出て、最終スコア・弾いた回数・最高コンボが
   表示されることを確認する。

## 今後の改善候補（未着手・任意）

- 弾き飛ばしの手応えのチューニング（`FLING_UP_*`, `MOTION_FULL_POWER_RATIO`）。
  部屋の明るさ・カメラ距離・カメラのフレームレートで最適値が変わりうる。
- コインの出現パターンのバリエーション追加（同時に複数落とすなど）。
- サウンド効果の追加（現状は無音）。弾いた瞬間の効果音があると手応えが増す。

## 設計上の注意点（引き継ぎ用）

- 依存ライブラリを追加しない方針を維持すること
  （どの Windows PC でもブラウザだけで動かすため）。
- カメラアクセスには secure context が必要なため、
  `file://` では動作しない前提を崩さないこと（README 参照）。
- 変更が必要な場合や UI 操作が必要な場合は、実装前後で
  ユーザーに一言説明すること（ユーザーからの明示的な依頼）。
