# 開発進捗メモ (シルエットコインゲット)

このファイルは、Claude Code などの AI エージェントがローカル環境で
作業を引き継ぐ際に読み込む前提の進捗管理ファイルです。
人が読んでも良いように日本語で状態を書いています。

## ゲーム仕様（最終目標）

- 制限時間60秒、コインを影で触るとスコア加算
- 敵（クリボー相当）に触るとスコア減、パワーダウン
- コインを一定数取ると BIG 状態になり得点2倍、色が変わる
- 感度と落下速度をスライダーで調整可能
- 特別なビルド手順・外部ライブラリ無しで、他の PC でもそのまま動く
  （Vanilla HTML/CSS/JS のみ。npm install 等が不要）

## ステージ構成と現在の状態

| Stage | 内容 | 状態 |
|---|---|---|
| 1 | カメラ取得 + 鏡像表示 | ✅ 実装済み |
| 2 | 背景記憶ボタン + 背景差分マスク可視化 | ⬜ 未実装 |
| 3 | ゲームロジック（落下物・当たり判定・スコア・タイマー・スライダー） | ⬜ 未実装 |

## Stage 1 の実装内容

- `index.html`: カメラ映像を表示するステージと開始/停止ボタンのみの UI。
- `game.js`:
  - `getUserMedia` でカメラ映像を取得。
  - `<video>` は非表示にし、`<canvas id="view">` に毎フレーム
    左右反転（鏡像）して描画する。
  - カメラ権限エラー時はステータス欄にエラーメッセージを表示する。
  - `getUserMedia` が使えない接続（file:// など）の場合は開始ボタンを
    無効化し、案内メッセージを出す。

### 動作確認方法（人間が実際にブラウザで確認する）

1. リポジトリのルートで簡易 HTTP サーバーを起動する
   （例: `python -m http.server 8000`）。
2. ブラウザで `http://localhost:8000/` を開く。
3. 「カメラ開始」ボタンを押し、カメラ権限を許可する。
4. 自分の映像が左右反転（鏡のように）表示されることを確認する。

## 次にやること (Stage 2)

1. 「背景を記憶」ボタンを追加し、押した瞬間の1フレームを
   背景画像としてオフスクリーン canvas に保存する。
2. 以降の毎フレームで、現在のフレームと背景画像の差分（ピクセルごとの
   輝度差）を計算し、閾値（感度）を超えたピクセルを「シルエット」と
   判定する。
3. シルエット判定結果を二値マスクとして可視化する
   （例: シルエット=白、背景=黒、を鏡像表示の上に半透明オーバーレイ、
   または隣に並べて表示）。
4. 感度の閾値は後の Stage 3 でスライダーに接続できるよう、
   コード上で変数として切り出しておく（マジックナンバーにしない）。
5. 実装後、人間が実際に体を動かして背景差分マスクが追従するかを
   ブラウザで確認してから Stage 3 に進む。

## 次にやること (Stage 3)

1. 落下物（コイン・敵）をランダム生成し、画面上部から一定速度で
   落下させる。
2. Stage 2 のシルエットマスクと落下物の座標を比較し、
   マスクが「シルエットあり」と判定するピクセル上に落下物が重なったら
   接触とみなす。
3. コイン接触: スコア加算。一定数取得で BIG 状態（得点2倍、色変化）。
4. 敵接触: スコア減算 + 一定時間のパワーダウン（例: 判定範囲縮小や
   スコア倍率低下など）。
5. 60秒の制限時間タイマーを実装し、0になったらゲーム終了画面を表示。
6. 感度（背景差分の閾値）・落下速度を調整できるスライダー UI を追加し、
   Stage 2 で切り出した変数、落下物の速度変数にそれぞれ接続する。

## 設計上の注意点（引き継ぎ用）

- 依存ライブラリを追加しない方針を維持すること
  （どの Windows PC でもブラウザだけで動かすため）。
- カメラアクセスには secure context が必要なため、
  `file://` では動作しない前提を崩さないこと（README 参照）。
- 変更が必要な場合や UI 操作が必要な場合は、実装前後で
  ユーザーに一言説明すること（ユーザーからの明示的な依頼）。
